spring:
  application:
    name: accounts
  datasource:
    url: jdbc:mysql://mysql_db_accounts:3306/db_accounts
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database: mysql
  sql:
    init:
      mode: always

message: hello, update busrefresh, webhook

management:
  endpoints:
    web:
      exposure:
        include: '*'
